{{ blok._editable }}
<section class="section {{ blok.style | join(' ') }}">
  <svg class="icon section__icon"><use xlink:href="#icon-bubble"></use></svg>
  <div class="container">
    {% for item in blok.body %}
      {% include item.component with blok: item %}
    {% endfor %}
  </div>
</section>
